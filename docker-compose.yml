services:
  landing:
    build: .
    image: chiket-landing:latest
    container_name: chiket-landing
    restart: unless-stopped
    ports:
      # Host port comes from .env. The fallback here only applies if that file
      # is missing, so both paths land on the same uncommon port.
      - "${PORT:-8417}:80"
    healthcheck:
      # localhost is ::1 in this image and nginx binds IPv4 only - name the address
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1/healthz"]
      interval: 30s
      timeout: 3s
      start_period: 5s
      retries: 3
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
